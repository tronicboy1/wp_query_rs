pub mod param_builder;
mod query;
mod sql;

#[allow(non_camel_case_types)]
pub struct WP_Query {}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        //let result = add(2, 2);
        // assert_eq!(result, 4);
    }
}
